# OpenAI API key — used by the vision extraction call in lib/openai/extractFields.ts.
# Copy this file to .env.local and fill in a real key for local development.
# Never commit .env.local. In production this is set as a Vercel environment variable.
OPENAI_API_KEY=
